import * as S from "./Day.styled"
import {WeatherData} from "../../weather";

interface DayProps{
    data:WeatherData[]
}
const Day = ({data}:DayProps) => {
    
    return (
        <S.Wrapper>
            
        </S.Wrapper>
    )
}

export default Day